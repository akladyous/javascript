function isNumerc(...args) {
    debugger
    let ar1 = [];
    this.forEach(v=> {
        console.log(v)
        ar1.push(/^-?\d+$/.test(v))
    })
    // return /^-?\d+$/.test(this)
};

isNumerc.apply([1,2,3])