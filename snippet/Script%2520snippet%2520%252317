function isNumerc(...args) {
    // debugger
    console.log([...this])
    let ar1 = [];
    this.forEach(v=> {
        // console.log(v)
        ar1.push(/^-?\d+$/.test(v))
    })
    // return /^-?\d+$/.test(this)
    return ar1.every(Boolean)
};

console.log(isNumerc.apply(1))